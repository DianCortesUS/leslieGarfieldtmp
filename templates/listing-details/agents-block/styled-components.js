import styled from 'styled-components';
import {Typography} from '@material-ui/core';

export const Name = styled(Typography)`
  font-family: sweet sans;
  text-transform: uppercase;
  font-size: 14px;
  font-weight: bold;
  line-height: 18px;
`;

export const AgentsPhone = styled(Typography)`
font-style: italic;
font-size: 13px;
`
